type: object
properties:
  success:
    type: boolean
    example: true
  payload:
    type: object
    properties:
      done:
        type: boolean
        example: true
        description: if true, it's already installed!
