type: object
allOf:
  - $ref: "../../user/_types/user.yaml"
  - type: object
    properties:
      userId:
        type: number
      tag:
        $ref: "../../shift/_types/tag.yaml"
    required:
      - userId
      - tag
