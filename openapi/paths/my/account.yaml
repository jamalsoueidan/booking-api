get:
  tags:
    - My Account
  operationId: myAccountGet
  summary: Get my-account data
  description: This endpoint get account data.
  responses:
    "200":
      description: Response
      content:
        application/json:
          schema:
            $ref: "../user/get-by-id/response.yaml"
    "403":
      $ref: "../../responses/forbidden.yaml"
  security:
    - BearerAuth: []

put:
  tags:
    - My Account
  operationId: myAccountUpdate
  summary: Update my-account data
  description: This endpoint update account
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../user/update/body.yaml"

  responses:
    "200":
      description: Response
      content:
        application/json:
          schema:
            $ref: "../user/update/response.yaml"
    "401":
      $ref: "../../responses/unauthorized.yaml"
    "403":
      $ref: "../../responses/forbidden.yaml"
    "404":
      $ref: "../../responses/not-found.yaml"
  security:
    - BearerAuth: []
