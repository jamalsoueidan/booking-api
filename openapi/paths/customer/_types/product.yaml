type: object
allOf:
  - $ref: ../../schedule/_types/product.yaml # Replace this with the path to your original schema
  - type: object
    properties:
      _id:
        type: string
      name:
        type: string

    required:
      - _id
      - name
