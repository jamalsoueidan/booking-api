type: object
properties:
  locationType:
    type: string
    enum: [origin, destination]
    default: destination
  customerId:
    type: string
    format: gid
  name:
    type: string
  fullAddress:
    type: string
required:
  - locationType
  - customerId
  - name
  - fullAddress
