type: object
properties:
  locationType:
    type: string
    enum: [origin, destination]
  minDistanceForFree:
    oneOf:
      - type: number
      - type: string
  distanceHourlyRate:
    oneOf:
      - type: number
      - type: string
  fixedRatePerKm:
    oneOf:
      - type: number
      - type: string
required:
  - locationType
  - minDistanceForFree
  - distanceHourlyRate
  - fixedRatePerKm
