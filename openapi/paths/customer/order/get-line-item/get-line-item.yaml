allOf:
  - $ref: "../_types/order.yaml"
  - type: object
    properties:
      line_items:
        allOf:
          - $ref: ../_types/line-item-with-lookup.yaml
          - type: object
            properties:
              selectedOptions:
                $ref: ../_types/user.yaml
            required:
              - selectedOptions
    required:
      - line_items
