get:
  parameters:
    - name: customerId
      in: path
      description: customerId for the customer
      required: true
      schema:
        type: string
    - name: id
      in: query
      description: id for the order
      required: true
      schema:
        type: string
  tags:
    - CustomerOrder
  operationId: customerOrderGetShipping
  summary: GET Get one order destinations for customer
  description: Get one driving information that are included in the order
  responses:
    "200":
      description: "Response"
      content:
        application/json:
          schema:
            $ref: "./response.yaml"
    "400":
      $ref: "../../../../responses/bad.yaml"
    "401":
      $ref: "../../../../responses/unauthorized.yaml"
    "403":
      $ref: "../../../../responses/forbidden.yaml"
    "404":
      $ref: "../../../../responses/not-found.yaml"

  security: []
