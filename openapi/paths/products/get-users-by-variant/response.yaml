type: object
properties:
  success:
    type: boolean
    example: true
  payload:
    type: object
    properties:
      productId:
        type: number
      variantId:
        type: number
      totalUsers:
        type: number
      nextCursor:
        type: string
      result:
        type: array
        items:
          $ref: "./user.yaml"
    required:
      - result
      - totalUsers
      - variantId
      - productId

required:
  - success
  - payload
