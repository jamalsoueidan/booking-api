get:
  parameters:
    - name: productIds
      in: query
      required: true
      schema:
        type: array
        items:
          type: string

  tags:
    - Products
  operationId: productsGetUsersImage
  summary: GET get users belongs to productIds array
  description: This endpoint respond are used with conjunction to show users profile image under treatments in the store
  responses:
    "200":
      description: "Response"
      content:
        application/json:
          schema:
            $ref: "./response.yaml"
    "400":
      $ref: "../../../responses/bad.yaml"
    "401":
      $ref: "../../../responses/unauthorized.yaml"
    "403":
      $ref: "../../../responses/forbidden.yaml"
  security: []
