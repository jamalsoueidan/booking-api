post:
  tags:
    - Shift
  operationId: createShift
  summary: POST Create shift
  description: This endpoint create new shift
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas/shift-create.yaml"

  responses:
    "200":
      $ref: "./responses/shift-response.yaml"
    "400":
      $ref: "../../responses/error.yaml"

  security:
    - BearerAuth: []
