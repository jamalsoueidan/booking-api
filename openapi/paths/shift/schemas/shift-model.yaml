type: object
properties:
  _id:
    type: string
  end:
    type: string
    format: date-time
  groupId:
    type: string
    nullable: true
  userId:
    type: string
  start:
    type: string
    format: date-time
  tag:
    type: string
    enum:
      - "weekday"
      - "weekend"
      - "all_day"
      - "end_of_week"
      - "start_of_week"
      - "middle_of_week"
required:
  - _id
  - end
  - userId
  - start
  - tag
