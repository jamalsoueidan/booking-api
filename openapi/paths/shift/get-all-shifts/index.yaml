parameters:
  - name: userId
    in: path
    description: The ID of the user
    required: true
    schema:
      type: string
  - name: start
    in: query
    description: start of the date
    required: true
    schema:
      type: string
      format: date-time
  - name: end
    in: query
    description: end of the date
    required: true
    schema:
      type: string
      format: date-time
get:
  tags:
    - Shift
  operationId: shiftGetAll
  summary: GET Get all shifts
  description: This endpoint get all shifts
  responses:
    "200":
      description: "Response ShiftDestroyGroup model"
      content:
        application/json:
          schema:
            $ref: "./response.yaml"
    "400":
      $ref: "../../../responses/bad.yaml"
    "401":
      $ref: "../../../responses/unauthorized.yaml"
    "403":
      $ref: "../../../responses/forbidden.yaml"

  security:
    - BearerAuth: []
