type: object
properties:
  end:
    type: string
    format: date-time
  start:
    type: string
    format: date-time
  tag:
    $ref: "../_types/tag.yaml"

required:
  - end
  - userId
  - start
  - tag
