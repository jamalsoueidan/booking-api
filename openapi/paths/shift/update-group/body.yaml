type: object
properties:
  end:
    type: string
    format: date-time
  start:
    type: string
    format: date-time
  days:
    $ref: "../_types/day.yaml"
  tag:
    $ref: "../_types/tag.yaml"

required:
  - end
  - userId
  - start
  - days
  - tag
