parameters:
  - name: group
    in: query
    description: The field filter by user group, this is automatically set when user is admin.
    required: false
    schema:
      type: string
get:
  tags:
    - Product
  operationId: getAllAvailableUsers
  summary: GET Get all available users
  description: This endpoint get all available users
  responses:
    "200":
      description: "Response"
      content:
        application/json:
          schema:
            $ref: "./response.yaml"
    "400":
      $ref: "../../../responses/error.yaml"

  security:
    - BearerAuth:
        - owner
        - admin
