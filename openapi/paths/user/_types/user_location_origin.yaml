type: object
properties:
  _id:
    type: string
  locationType:
    type: string
    enum: [origin, destination]
  customerId:
    type: string
    format: gid
  destinationType:
    type: string
    enum: [home, commercial]
  name:
    type: string
  fullAddress:
    type: string
  geoLocation:
    type: object
    required:
      - type
      - coordinates
    properties:
      type:
        type: string
        enum: ["Point"]
      coordinates:
        type: array
        items:
          type: number
  isDefault:
    type: boolean
required:
  - _id
  - locationType
  - customerId
  - destinationType
  - name
  - fullAddress
  - geoLocation
  - isDefault
