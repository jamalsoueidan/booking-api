put:
  tags:
    - User
  operationId: updateUser
  summary: PUT Update user
  description: This endpoint update user
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas/user-create.yaml"

  responses:
    "200":
      $ref: "./responses/user-response.yaml"
    "400":
      $ref: "../../responses/error.yaml"
  security:
    - BearerAuth:
        - owner
        - admin
