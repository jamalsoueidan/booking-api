get:
  parameters:
    - name: search
      in: query
      schema:
        type: string
      required: true
  tags:
    - User
  operationId: usersSearch
  summary: GET Get all users using search by username or fullname
  description: This endpoint get all users
  responses:
    "200":
      description: "Response"
      content:
        application/json:
          schema:
            $ref: "./response.yaml"
    "400":
      $ref: "../../../responses/bad.yaml"
    "401":
      $ref: "../../../responses/unauthorized.yaml"
    "403":
      $ref: "../../../responses/forbidden.yaml"
    "404":
      $ref: "../../../responses/not-found.yaml"

  security: []
