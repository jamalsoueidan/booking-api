# To use this library just require this spec file, and your spec is connected to db.

Then just use the models normally.

```ts
require("@jamalsoueidan/bit-dev.testing-library.mongodb/mongodb.jest");
```
